version: "3.7"
services:
  webapp:
    build: ./app
    ports:
      - 8080:8080
    volumes:
      - ./DB/:/usr/src/app/DB/
    restart:  unless-stopped

  scraper:
    build: ./scraper
    volumes:
      - ./DB/:/DB/
    restart:  unless-stopped


